# =============================================================================
# CRYPTO TRADING BOT - ENVIRONMENT CONFIGURATION
# =============================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the .env file to version control (it's in .gitignore)
# 4. For production, use environment variables or secure secret management
#
# SECURITY WARNING:
# - Keep your API keys and secrets secure
# - Use demo/sandbox accounts for testing
# - Enable IP whitelisting on your exchange API keys
# - Use minimal required permissions (Read + Trade, no Withdraw)
# =============================================================================

# Database Configuration
POSTGRES_URL=postgresql://trading_user:your_password@localhost:5433/trading_bot
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=trading_bot
POSTGRES_USER=trading_user
POSTGRES_PASSWORD=your_password

# OKX API Configuration
# Get your API credentials from OKX exchange:
# 1. Go to OKX -> Account -> API Management
# 2. Create new API key with trading permissions
# 3. For demo trading, use sandbox/demo API keys
OKX_API_KEY=your_okx_api_key_here
OKX_SECRET_KEY=your_okx_secret_key_here
OKX_PASSPHRASE=your_okx_passphrase_here
OKX_IS_DEMO=true

# Telegram Configuration
# Get bot token from @BotFather on Telegram
# Get chat ID by messaging your bot and checking updates
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Docker Environment
DOCKER_ENV=true

# Trading Configuration (Legacy - now managed in database)
# These settings are kept for backward compatibility
# Use multi_crypto_config_cli.py to configure trading pairs and strategies
TRADING_PAIR=BTC/USDT
INITIAL_BALANCE=10000
TRADE_SIZE_USD=100
PAPER_TRADING=true
CHECK_INTERVAL=60

# Strategy Parameters (Legacy - now managed in database)
# Use multi_crypto_config_cli.py to configure strategy parameters
SHORT_SMA_PERIOD=10
LONG_SMA_PERIOD=30
RSI_PERIOD=14
RSI_OVERBOUGHT=70
RSI_OVERSOLD=30
STOP_LOSS_PERCENT=5.0
TAKE_PROFIT_PERCENT=10.0